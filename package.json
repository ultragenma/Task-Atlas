{
  "name": "physical-ai-task-atlas",
  "version": "0.2.0",
  "private": true,
  "description": "Explore Physical AI tasks by context, inspect claim-specific evidence, and export collection plans.",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node --watch backend/server.js",
    "validate": "node scripts/validate_seed_data.js",
    "validate:research": "node scripts/validate_research.js",
    "test": "node --test",
    "check": "npm run validate && npm run validate:research && npm test"
  }
}
